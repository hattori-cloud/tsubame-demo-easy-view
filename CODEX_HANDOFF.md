# CODEX 引継ぎ資料 — つばめ交通 社員一元管理システム

更新日: 2026-09-24  
対象リリース: **v198**  
対象リポジトリ: **hattori-cloud/tsubame-demo-easy-view**  
対象ブランチ: **main**  
本番URL: **https://tsubame-demo-easy-view.vercel.app**  
v197アプリ本体コミット: **3068c4fb383e6a7025a12b243e67176048ac4327**  
v197監査修正記録: README / tests / production-schema.sql を main に反映済み

---

## 0-A. v198 原本管理セキュリティ

v198では原本管理を通常の書類管理より強い権限モデルへ変更しています。

- デモにはPDF・画像・スキャン原本のアップロード欄を設けない
- 書類に「標準 / 要配慮 / 厳格」の機密区分を追加
- 「本人閲覧可 / 担当管理者まで / 全社管理者のみ」の閲覧範囲を追加
- 厳格書類は全社管理者のみ閲覧可能
- 原本確認済みへの変更は全社管理者のみ
- 原本区分、紙原本保管場所、保管期限を管理
- 旧版は削除せず replaced_from / replaced_by で差替え履歴を保持
- 「削除」は物理削除ではなく監査付き無効化へ変更
- 書類情報閲覧も監査ログへ記録
- 書類・原本管理センターは未確認 / 差替え待ち / 期限 / 厳格書類の4KPIで簡潔表示
- 本番DBに storage_version_id / SHA-256 / malware scan 状態を追加
- 本番はMFA、サーバー側認可、非公開暗号化ストレージ、60秒署名URL、マルウェア検査、閲覧/ダウンロード監査を必須とする

詳細: `docs/original-document-security-v198.md`

重要: v198でもDOCUMENT_STORAGE_CONFIG.connected=falseです。
実社員の原本ファイルは、本番認証・MFA・サーバー側権限・非公開ストレージ・監査・バックアップが接続されるまで投入禁止です。

---

## 0. v197 監査修正の現在地

2026-09-24のCODEX監査で指摘されたHigh項目に対し、v197で以下を修正しています。

- ヒヤリの教育事項・記入日・分類だけの変更でも保存対象として検出
- 通常保存と自動ロールバックが両方失敗した場合、以後の保存を停止する recovery-required 状態を追加
- 勤務集計Excelの空欄時間を0扱いせず、存在しない年月・日付をブロック
- 月次事故・ヒヤリ遷移時に古い検索条件を解除
- 全期間の高危険ヒヤリを開く時は月フィルターを解除
- ヒヤリ月2件対象者を月別スナップショットとして固定保存
- 異動・退職後の過去月集計は保存済みスナップショットを優先し、未作成月は入社日・異動履歴から月初状態を復元
- 過去月の未達者からヒヤリ登録を開く場合、記入日が別月ならその月の不足件数には入らないことを明示
- スマホの月次優先一覧は3件に制限し、事故・ヒヤリ未達・高危険が一種類に埋もれないよう分散
- 安全記録保存後の全画面再描画をやめ、表示中ページ中心の再描画へ変更
- docs/production-schema.sql を20テーブルの正規化済み基準スキーマへ置換
- 監査再発防止テストを tests/audit-regressions-v197.test.js / tests/schema-sanity.test.js に追加

重要: **v197はデモ監査のHigh修正であり、実社員データを扱う本番承認ではありません。**
実認証、MFA、サーバー側認可、共有DB、原本ストレージ、バックアップ/復元訓練は引き続き本番移行の停止条件です。

---

## 1. 最初に読んでほしいこと

このシステムは、タクシー・バス会社の管理者が社員情報、運行・安全、期限、車両、資格、教育、貸与品、苦情、申請等を一元確認するための社内管理システムのデモ／本番移行準備版です。

現在は「機能を増やす」だけではなく、現場管理者が **見やすく・分かりやすく・迷わず次の処理へ進める** ことを強く重視しています。

CODEXには、まず大きな書き換えをせず、現状コードの監査を優先してほしいです。特に以下を確認してください。

1. データ消失・保存不整合につながるバグ
2. 権限・担当範囲の迂回
3. クリックしても動かない／戻れない導線
4. 保存後の誤表示・二重登録につながる挙動
5. PC／スマホの表示崩れ
6. 事故・苦情・ヒヤリ・期限・車両間のリンク不整合
7. localStorageの移行・保存・復旧処理の安全性
8. 本番API設計とデモUIの前提の食い違い
9. JavaScript構文・未定義関数・DOM ID重複等の回帰
10. 実社員データ投入前に塞ぐべき本番移行ブロッカー

**最初の監査では、全面リファクタリングより「重大度付きの指摘」を優先してください。**

---

## 2. 絶対に守る前提

- **実社員情報を作成・推測・補完しないこと。**
- 現在の120名程度の社員データは架空の初期シードです。
- 実ファイル・実社員書類をこのデモへ勝手に投入しないこと。
- 号車は会社運用上の **3桁の号車** を基本識別子として扱っています。
- 事業所と部署は別軸です。
- 本社／府中は複数部署を持ち得ます。
- 馬木はバス課のみ、という会社ルールをコード側で扱っています。
- タクシー乗務員には **ゴールド認定・英語ドライバー・観光ドライバー** の社内認定があります。
- ゴールド認定は2026年10月運用開始予定として扱い、詳細基準を勝手に固定しない設計です。
- ヒヤリハットは事故・苦情のような「管理案件化」を目的にせず、傾向分析・安全教育向けの報告として扱います。
- 事故修理費は記録しますが、賞与控除額・控除状況をシステムに持たせる仕様ではありません。
- 健康情報は期限管理を中心にし、医療結果を不用意に表示しない方針です。

---

## 3. 現在の構成

### 主要ファイル

- **index.html**
  - 現在の主アプリ本体。
  - HTML / CSS / JavaScriptがほぼ1ファイルに集約されています。
  - 約840KB規模。
  - 現在のUI・架空データ・保存・権限・分析・監査機能の大部分がここにあります。

- **README.md**
  - v107以降を中心に、各リリースで何を変更したかを時系列で記録。
  - 現状把握に非常に重要です。

- **docs/api-contract.md**
  - 本番APIの契約・設計方針。

- **docs/production-schema.sql**
  - 本番DB候補スキーマ。
  - 現在のデモlocalStorage構造とは別に、本番移行を想定したSQL設計があります。

- **docs/v107-api-foundation.md**
  - 本番API基盤の初期方針。

### API基盤

- api/_lib/auth.js
- api/_lib/authorization.js
- api/_lib/security.js
- api/_fixtures/staging-registry.js
- api/v1/health.js
- api/v1/me.js
- api/v1/secure-probe.js
- api/v1/employees/[id].js

本番APIはまだ「完成した本番業務API」ではありません。  
特に secure-probe 等は **実認証が完成するまではfail-closed** を基本とする設計です。

---

## 4. 現在のデータ保存

デモ本体は主にブラウザの **localStorage** を利用しています。

重要関数:

- `coreSaveItems()`
- `captureCoreSaveSnapshot()`
- `verifyCoreSave()`
- `rollbackCoreSave()`
- `save()`

`save()` は単純な localStorage.setItem の羅列ではなく、

1. 保存前スナップショット取得
2. コアデータ一括書込
3. 書込後の再読込・一致確認
4. 失敗時のロールバック

という考え方で実装しています。

主要コレクションには以下があります。

- `E` 社員
- `A` 事故
- `V` 車両
- `N` ヒヤリハット
- `C` 苦情
- `TR` 安全指導・研修
- `AS` 貸与品
- `QUAL` 資格
- `GUIDANCE` 面談・指導
- `DOCS` 書類
- `PROCEDURES` 休職・退職等手続
- `AUDIT` 操作監査
- `APPLICATIONS` 申請
- `NOTICES` 掲示
- `CONFIRMATIONS` 確認事項
- `HANDOFFS` 引継ぎ

localStorageキーは `coreSaveItems()` を正として確認してください。

---

## 5. 権限モデル

現在のデモUIでは主に以下の3モードを持ちます。

- full: 全社管理者
- scoped: 担当範囲管理者
- self: 一般社員本人

重要関数:

- `setPreviewRole()`
- `isFullCompanyAdmin()`
- `currentAdminAssignedScopes()`
- `employeeMatchesAssignedScope()`
- `grantedEmployees()`
- `scopedEmployees()`
- `canAccess()`
- `canEditEmployee()`
- `guardManagementRecord()`
- `guardEmployeeEdit()`

**注意:** これはデモでの権限プレビューを含みます。  
実社員データを扱う本番では、フロント側判定だけでは不十分です。READMEとAPI設計にある通り、認証・MFA・サーバー側認可・DB制約・監査が必要です。

CODEX監査では、フロント側で担当範囲外データへ到達できないかだけでなく、本番API設計との境界も確認してください。

---

## 6. 主要画面と重要関数

### ホーム
目的: 朝30秒程度で今日の優先作業を把握。

主な機能:
- 全体検索
- 今日の要確認
- 期限超過
- 事故・苦情の未完了
- 勤務注意
- よく使う登録
- 最近開いた詳細

### 社員
重要関数:
- `renderEmp()`
- `employeeDetail()`
- `openEmployeeEdit()`
- `employeeNextActionInfo()`
- `employeeDetailRouteHtml()`

社員名クリックは原則社員詳細へ飛ぶ設計です。

### 事故
重要関数:
- `renderAcc()`
- `accidentDetail()`
- `openAccidentForm()`
- `openAccidentEdit()`
- `accidentAttentionInfo()`
- `accidentManagementChecklistHtml()`

事故完了では、原因・再発防止・対応履歴等の入力条件を確認します。

### 苦情
重要関数:
- `renderComplaints()`
- `complaintDetail()`
- `openComplaintForm()`
- `complaintAttentionInfo()`
- `complaintManagementChecklistHtml()`

未完了では担当・期限・次回対応・ランク等を確認します。

### ヒヤリハット
重要関数:
- `renderNear()`
- `nearDetail()`
- `openNearForm()`
- `nearAttentionInfo()`
- `nearAnalysisChecklistHtml()`

管理案件化ではなく、危険度・原因側・状況分類・対策・教育事項の分析充足を重視します。

### 車両
重要関数:
- `renderVehicles()`
- `vehicleDetail()`
- `openVehicleForm()`
- `vehicleNextActionInfo()`
- `vehicleOperationalChecklistHtml()`

3桁号車を中心に社員・事故・苦情・ヒヤリをリンクします。

### 期限
重要関数:
- `collectDeadlines()`
- `openDeadlineCenter()`
- `renderDeadlineCenterRows()`
- `deadlineOpen()`
- `deadlineQuickEdit()`
- `deadlineReasonText()`

### 共通の「次の操作」
重要関数:
- `commonNextActionPanelHtml()`
- `caseActionPanelHtml()`
- `detailBackButtonHtml()`
- `showSaveToast()`
- `showPostSaveGuide()`

---

## 7. v169〜v173 の直近変更

### v169
- 保存後に共通の「保存しました」表示を追加。

### v170
- 単にフォームが閉じただけでは保存成功表示を出さないよう修正。
- `save()` が実際に成功した場合のみ保存完了表示。
- 保存後詳細に「次にやることを見る」導線を追加。
- 主要クリックハンドラ監査を実施。

### v171
社員・事故・苦情・ヒヤリ・車両・期限の判断表示を原則、

**なぜ確認？ → 次にやること → 担当 → 期限・状態**

の順に統一。

### v172
事故・苦情・ヒヤリ・車両詳細の上部操作を統一。

- 「← 1つ前へ」
- 「← 元の画面へ」
- 一覧へ
- 社員詳細へ
- 修正

スマホでは修正ボタンを大きく表示し、誤タップを減らす配置へ変更。

### v173
一覧側の主要操作文言を統一。

- **見る**: 内容確認
- **対応する**: 要対応レコードへ進む
- **修正する**: 直接編集

対象:
- 社員
- 事故
- 苦情
- ヒヤリ
- 車両
- 期限センター

権限やデータロジックそのものは変更していません。

---

## 8. 現時点の監査結果

v173反映後に以下を確認済みです。

- index.html 内のJavaScript構文チェック: **正常**
- インライン操作ハンドラ数: **569件**
- 参照されているアプリ独自関数の未定義: **0件**
- Vercel production deploy: **READY**
- 本番URL応答: **HTTP 200**
- 直近Vercel runtime errors: v172時点まで **0件**
- v173アプリ本体コミット: `f61da6c7bb1314a4ffa8d6867993f93fae6d4435`

CODEX側でも再監査してください。特に静的解析だけでなく、実ブラウザ操作で主要導線を確認してほしいです。

---

## 9. CODEXに優先して確認してほしいシナリオ

### A. 保存
- 社員編集
- 事故登録／編集
- 苦情登録／編集
- ヒヤリ登録／編集
- 車両編集
- 資格・書類編集

確認事項:
- 必須不足で保存されない
- 成功していないのに「保存しました」が出ない
- 成功時に一度だけ保存される
- 保存後の表示先が正しい
- localStorage書込失敗時にロールバックできる

### B. ナビゲーション
- ホーム検索 → 社員詳細 → 事故詳細 → 社員詳細 → 戻る
- 期限センター → 事故／苦情／車両 → 戻る
- 号車 → 車両詳細 → 関連安全記録 → 戻る
- 社員詳細 → 資格／書類 → 戻る

確認事項:
- DETAIL_HISTORY
- DETAIL_ORIGIN
- 検索結果や元画面の復元
- 「1つ前へ」「元の画面へ」の表示整合

### C. 権限
full / scoped / self それぞれで:
- 担当外社員
- 担当外事故
- 担当外苦情
- 担当外車両
- 書類
- 管理機能

への直接関数呼出し・リンク遷移も確認。

### D. スマホ
幅 390px 前後を中心に:
- 社員一覧
- 事故
- 苦情
- ヒヤリ
- 車両
- 期限センター
- 各詳細
- 長い入力フォーム

を確認。

### E. データ整合
- 社員番号参照
- 3桁号車
- 資格 ↔ 書類
- 車両 ↔ 主担当／追加利用社員
- 安全記録 ↔ 社員
- HANDOFFS ↔ 案件
- AUDIT ↔ 更新対象

---

## 10. 既知の大きな制約

1. **index.htmlが巨大なモノリス**
   - 変更しやすい反面、回帰リスクがあります。
   - 将来的な分割は有効ですが、いきなり全面分割すると現行挙動を壊す可能性があります。

2. **デモ業務データはlocalStorage**
   - 本番DBではありません。
   - 複数端末・複数ユーザー共有の本番運用にはそのまま使えません。

3. **実認証は未完成**
   - デモの役割切替と本物のログイン認証を混同しないこと。

4. **本番APIは基盤途中**
   - 健康確認等の基礎APIとfail-closed設計はありますが、全業務CRUDが本番完成しているわけではありません。

5. **実ファイル保管未完成**
   - 書類管理は現在主にメタデータ管理です。

6. **本番移行前ゲート**
   - 認証
   - MFA
   - サーバー側認可
   - DB
   - バックアップ
   - 監査
   - 保持期間
   - 実社員データ照合
   - デバイスUAT
   - 外部連携
   等が本番移行条件です。

---

## 11. 監査時の変更方針

CODEXが問題を見つけた場合、次の順で提案してください。

1. Critical: データ消失、権限漏れ、保存破壊、起動不能
2. High: 主要操作不能、誤った社員／案件へ遷移、誤保存
3. Medium: UIの誤解、モバイル操作性、管理者判断の分かりにくさ
4. Low: 表記揺れ、重複CSS、整理可能な技術負債

**まず問題と再現手順を提示し、その後に最小修正案を出すのが望ましいです。**

既存仕様を壊す大規模リファクタリングは、監査結果を確認してからにしてください。

---

## 12. 現在のUX方針

現場管理者向けなので、画面を開いた時に「情報量の多さ」より以下を優先しています。

- なぜ確認するのか
- 次に何をするのか
- 誰が担当か
- いつまでか
- どこを押せばいいか
- どこへ戻るか

事故・苦情・期限等は「管理者が迷わず処理できる」ことを最優先。

ヒヤリは「案件処理」ではなく「安全分析・教育への活用」を優先。

---

## 13. CODEXへの最初の依頼文（そのまま使えます）

> このリポジトリは、つばめ交通の社員一元管理システム v173 です。まずコードを変更せず監査してください。README.md と CODEX_HANDOFF.md を読み、index.html、api/、docs/ を確認してください。特に、データ消失、localStorage保存／ロールバック、権限迂回、未定義イベントハンドラ、詳細画面の戻り履歴、事故・苦情・ヒヤリ・車両・期限間のリンク、スマホ操作、本番API設計との矛盾を重点確認してください。実社員情報を作成・推測しないでください。重大度 Critical / High / Medium / Low、再現手順、原因、最小修正案の順で報告してください。全面リファクタリングはまだ行わないでください。

---

## 14. 参照順序

CODEXはまず以下の順に確認すると把握しやすいです。

1. `CODEX_HANDOFF.md`
2. `README.md`
3. `index.html`
4. `docs/api-contract.md`
5. `docs/production-schema.sql`
6. `api/_lib/auth.js`
7. `api/_lib/authorization.js`
8. `api/_lib/security.js`
9. `api/v1/`

---

## 15. 現在地

現状は、**デモ土台としてはかなり機能が揃っているが、実社員データを扱う本番システムとしては認証・サーバー認可・DB等の本番基盤が未完成**、という位置づけです。

UI改善を続けつつ、本番移行準備を明確に分離して進めています。

次の開発者は、デモ画面が成熟していることを「本番準備完了」と誤認しないでください。
